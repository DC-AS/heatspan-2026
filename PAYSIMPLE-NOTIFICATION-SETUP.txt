HEATSPAN PARTS PROTECTION — STEP 2A COMPLETE

STATUS
- Website/Vercel webhook code is prepared.
- Notifications are intentionally DISABLED until Step 2B.
- No Network Solutions or heatspan.com DNS changes are required for Step 2A.

WHAT STEP 2A DOES
- Accepts only POST webhook requests.
- Requires PaySimple webhook signature verification once activated.
- Filters for payment_created + embeddable checkout + Heatspan plan prices ($28.60 / $342.96).
- Ignores later recurring subscription charges.
- Sends the purchase alert to info@heatspan.com through Resend.
- Excludes card and bank-account details from notification emails.
- Escapes webhook-supplied values before inserting them into email HTML.
- Limits webhook payload size.
- Uses a Resend Idempotency-Key based on the payment/order/event so webhook retries do not normally create duplicate emails within Resend's idempotency window.
- Fails closed: if activation or the webhook secret is missing, no notification is sent.

STEP 2B — DO LATER
1. In PaySimple, create/enable the appropriate payment_created webhook subscription for the deployed endpoint:
   https://YOUR-VERCEL-DEPLOYMENT.vercel.app/api/paysimple-protection-webhook
2. Copy the PaySimple webhook signing secret into Vercel as:
   PAYSIMPLE_WEBHOOK_SECRET
3. Confirm Vercel has:
   RESEND_API_KEY
   PARTS_NOTIFICATION_TO=info@heatspan.com
4. Set PAYSIMPLE_NOTIFICATIONS_ENABLED=true only when ready to activate/test the webhook.
5. Use a verified Resend sender/domain for production delivery when Step 1B is activated.
6. Perform controlled monthly and annual end-to-end tests and confirm the alert reaches info@heatspan.com.

IMPORTANT
Do not put card numbers, bank account numbers, routing numbers, or other payment credentials in notification emails or logs.
Do not activate the webhook until the PaySimple signing secret is configured.
