HEATSPAN STEP 9.1 — HERO FRAMING CORRECTION

This revision specifically addresses the remaining zoom/crop mismatch.

Changes:
- New wider crops include significantly more surrounding scene context.
- Hero artwork now uses object-fit: contain, not cover.
- The browser is no longer allowed to crop/zoom the service artwork.
- The media panel is slightly wider to match the approved mockup proportions.
- Text column is slightly narrower to give the art the same breathing room shown in the mockups.
- Home/index.html remains unchanged.
- Existing navigation, weather strip, backend, and internal page content are preserved.

Recommended commit:
Correct service hero framing to match mockups
